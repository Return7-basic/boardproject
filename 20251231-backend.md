# 백엔드 변경 사항 (2025-12-31)

## 📋 개요
프론트엔드 작업 중 발생한 이슈 해결 및 기능 개선을 위한 백엔드 수정 사항입니다.

---

## 🔧 수정된 파일 목록

### 1. **ReplyController.java**
**경로**: `board-backend/src/main/java/return7/boardbackend/controller/ReplyController.java`

#### 변경 사항:

**1.1. 댓글 조회 API 기본값 변경**
- **위치**: `getReply()` 메서드
- **변경 내용**: `sort` 파라미터 기본값을 `"latest"`에서 `"ascending"`으로 변경
- **주석**: `// FE 작업중 수정 : sort 기본값을 "latest"에서 "ascending"으로 변경 (기본순 오름차순)`
- **코드**:
```java
@RequestParam (defaultValue = "ascending")String sort
```

**1.2. 채택된 댓글 조회 API 개선**
- **위치**: `getSelectedReply()` 메서드
- **변경 내용**: 채택된 댓글이 없을 때 404 반환하도록 Optional 처리 추가
- **주석**: `// FE 작업중 수정 : 채택된 댓글이 없을 때 404 반환하도록 Optional 처리 추가`
- **코드**:
```java
@GetMapping("/selected")
public ResponseEntity<ResponseReplyDto> getSelectedReply (@PathVariable Long boardId) {
    return replyService.getSelectedReply(boardId)
            .map(ResponseReplyDto -> ResponseEntity.ok(ResponseReplyDto))
            .orElse(ResponseEntity.notFound().build());
}
```

**1.3. Soft delete된 댓글 정리 API 추가**
- **위치**: `cleanupDeletedReplies()` 메서드 (신규)
- **변경 내용**: 관리자만 사용 가능한 cleanup 엔드포인트 추가
- **주석**: `// FE 작업중 수정 : Soft delete된 댓글들 정리 엔드포인트 추가 (관리자만 사용 가능)`
- **코드**:
```java
@PostMapping("/cleanup")
public ResponseEntity<Integer> cleanupDeletedReplies(
        @PathVariable Long boardId,
        @AuthenticationPrincipal CustomPrincipal customPrincipal) {
    boolean isAdmin = customPrincipal.getAuthorities().stream()
            .anyMatch(a -> a.getAuthority().equals("ROLE_ADMIN"));
    if (!isAdmin) {
        return ResponseEntity.status(403).build();
    }
    
    int deletedCount = replyService.cleanupDeletedReplies();
    return ResponseEntity.ok(deletedCount);
}
```

---

### 2. **ReplyRepository.java**
**경로**: `board-backend/src/main/java/return7/boardbackend/repository/ReplyRepository.java`

#### 변경 사항:

**2.1. 오름차순 조회 메서드 추가**
- **위치**: 인터페이스 메서드
- **변경 내용**: 기본순 정렬을 위한 오름차순 조회 메서드 추가
- **주석**: `// FE 작업중 수정 : 오름차순 조회 메서드 추가 (기본순 정렬용)`
- **코드**:
```java
List<Reply> findByBoardIdOrderByIdAsc(Long boardId, Pageable pageable);
List<Reply> findByBoardIdAndIdGreaterThanOrderByIdAsc(Long boardId, Long cursorId, Pageable pageable);
```

**2.2. 부모 댓글의 삭제되지 않은 자식 개수 조회 메서드 추가**
- **위치**: `@Query` 어노테이션 메서드
- **변경 내용**: 부모 댓글 자동 삭제 로직을 위한 메서드 추가
- **주석**: `// FE 작업중 수정 : 부모 댓글의 삭제되지 않은 자식 개수 조회 메서드 추가 (부모 자동 삭제 로직용)`
- **코드**:
```java
@Query("SELECT COUNT(r) FROM Reply r WHERE r.parent.id = :parentId AND r.isDeleted = false")
long countByParentIdAndIsDeletedFalse(@Param("parentId") Long parentId);
```

**2.3. Soft delete된 댓글 조회 메서드 추가**
- **위치**: `@Query` 어노테이션 메서드
- **변경 내용**: cleanup 기능을 위한 메서드 추가
- **주석**: `// FE 작업중 수정 : Soft delete된 모든 댓글 조회 메서드 추가 (cleanup 기능용)`
- **코드**:
```java
@Query("SELECT r FROM Reply r WHERE r.isDeleted = true")
List<Reply> findAllDeletedReplies();
```

---

### 3. **ReplyService.java**
**경로**: `board-backend/src/main/java/return7/boardbackend/service/ReplyService.java`

#### 변경 사항:

**3.1. 댓글 삭제 로직 개선 (부모 자동 삭제)**
- **위치**: `delete()` 메서드
- **변경 내용**: 부모가 soft delete된 상태이고 모든 자식이 삭제되면 부모도 자동 hard delete
- **주석**: `// FE 작업중 수정 : 부모가 soft delete된 상태이고 모든 자식이 삭제되면 부모도 자동 hard delete`
- **코드**:
```java
// 부모가 있고 부모가 soft delete된 상태라면, 부모의 남은 자식 확인
if (parentId != null) {
    Optional<Reply> parentOptional = replyRepository.findById(parentId);
    if (parentOptional.isPresent()) {
        Reply parentReply = parentOptional.get();
        if (parentReply.isDeleted()) {
            long notDeletedChildrenCount = replyRepository.countByParentIdAndIsDeletedFalse(parentId);
            if (notDeletedChildrenCount == 0) {
                replyRepository.delete(parentReply);
            }
        }
    }
}
```

**3.2. Soft delete된 댓글 정리 메서드 추가**
- **위치**: `cleanupDeletedReplies()` 메서드 (신규)
- **변경 내용**: 자식이 모두 삭제된 soft delete 댓글들을 완전히 삭제하는 메서드 추가
- **주석**: `// FE 작업중 수정 : Soft delete된 댓글들 정리 메서드 추가 (데이터 정리용)`
- **코드**:
```java
@Transactional
public int cleanupDeletedReplies() {
    List<Reply> deletedReplies = replyRepository.findAllDeletedReplies();
    int deletedCount = 0;
    
    for (Reply deletedReply : deletedReplies) {
        long notDeletedChildrenCount = replyRepository.countByParentIdAndIsDeletedFalse(deletedReply.getId());
        if (notDeletedChildrenCount == 0) {
            replyRepository.delete(deletedReply);
            deletedCount++;
        }
    }
    
    return deletedCount;
}
```

**3.3. 댓글 조회 정렬 로직 개선**
- **위치**: `getReplyByBoard()` 메서드
- **변경 내용**: 정렬 옵션 추가 (ascending: 기본순 오름차순, latest: 최신순 내림차순, 그 외: 추천순)
- **주석**: `// FE 작업중 수정 : 정렬 옵션 추가 (ascending: 기본순 오름차순, latest: 최신순 내림차순, 그 외: 추천순)`
- **코드**:
```java
if ("latest".equalsIgnoreCase(sort.trim())){
    // 최신순 - 내림차순 (ID DESC)
    replies = (cursorId == null)
            ? replyRepository.findByBoardIdOrderByIdDesc(boardId, pageable)
            : replyRepository.findByBoardIdAndIdLessThanOrderByIdDesc(boardId, cursorId, pageable);
} else if ("ascending".equalsIgnoreCase(sort.trim())){
    // 기본순 - 오름차순 (ID ASC)
    replies = (cursorId == null)
            ? replyRepository.findByBoardIdOrderByIdAsc(boardId, pageable)
            : replyRepository.findByBoardIdAndIdGreaterThanOrderByIdAsc(boardId, cursorId, pageable);
} else {
    // 추천순 - 추천수 내림차순, ID 내림차순
    replies = (cursorId == null)
            ? replyRepository.findByBoardIdOrderByRecommendationDescIdDesc(boardId, pageable)
            : replyRepository.findByBest(boardId,cursorScore, cursorId, pageable);
}
```

**3.4. nextScore 로직 수정**
- **위치**: `getReplyByBoard()` 메서드 내부
- **변경 내용**: nextScore는 추천순일 때만 설정하도록 로직 수정 (ascending 추가)
- **주석**: `// FE 작업중 수정 : nextScore는 추천순일 때만 설정하도록 로직 수정 (ascending 추가)`
- **코드**:
```java
if (!"latest".equalsIgnoreCase(sort.trim()) && !"ascending".equalsIgnoreCase(sort.trim())) {
    // 추천순인 경우에만 nextScore 설정
    nextScore = replies.get(replies.size() - 1).getRecommendation();
} else {
    // 최신순 또는 기본순인 경우 nextScore는 null
    nextScore = null;
}
```

**3.5. 채택된 댓글 조회 메서드 개선**
- **위치**: `getSelectedReply()` 메서드
- **변경 내용**: 반환 타입을 `ResponseReplyDto`에서 `Optional<ResponseReplyDto>`로 변경
- **주석**: `// FE 작업중 수정 : 반환 타입을 ResponseReplyDto에서 Optional<ResponseReplyDto>로 변경하여 채택된 댓글이 없을 때 404 처리 가능하도록 수정`
- **코드**:
```java
@Transactional(readOnly = true)
public Optional<ResponseReplyDto> getSelectedReply(Long boardId) {
    boardRepository.findById(boardId)
            .orElseThrow(() -> new BoardNotFoundException("게시물을 찾을 수 없습니다."));

    Optional<Reply> replyOptional = replyRepository.findByBoardIdAndIsSelectedTrue(boardId);
    
    return replyOptional.map(ResponseReplyDto::from);
}
```

---

### 4. **ResponseReplyDto.java**
**경로**: `board-backend/src/main/java/return7/boardbackend/dto/reply/ResponseReplyDto.java`

#### 변경 사항:

**4.1. isDeleted 필드 JSON 직렬화 개선**
- **위치**: `isDeleted` 필드
- **변경 내용**: `@JsonProperty("isDeleted")` 어노테이션 추가하여 JSON 직렬화 일관성 보장
- **주석**: `// FE 작업중 수정 : isDeleted 필드에 @JsonProperty 추가하여 JSON 직렬화 일관성 보장`
- **코드**:
```java
@JsonProperty("isDeleted")
private boolean isDeleted;
```

---

### 5. **BoardService.java**
**경로**: `board-backend/src/main/java/return7/boardbackend/service/BoardService.java`

#### 변경 사항:

**5.1. 게시글 목록 조회 반환 타입 변경**
- **위치**: `findAll()` 메서드
- **변경 내용**: 반환 타입을 `List<BoardDto>`에서 `BoardListResponseDto`로 변경하여 `hasNext` 정보 제공
- **주석**: `// FE 작업중 수정 : 반환 타입을 List<BoardDto>에서 BoardListResponseDto로 변경하여 hasNext 정보 제공`
- **코드**:
```java
@Transactional(readOnly = true)
public BoardListResponseDto findAll(int page, int size) {
    Pageable pageable = PageRequest.of(page, size, Sort.by("id").descending());
    Page<Board> boardPage = boardRepository.findAll(pageable);
    
    List<BoardDto> boardDtos = boardPage.getContent()
            .stream()
            .map(BoardDto::from)
            .collect(Collectors.toList());
    
    boolean hasNext = boardPage.hasNext();
    
    return new BoardListResponseDto(boardDtos, hasNext);
}
```

---

### 6. **BoardController.java**
**경로**: `board-backend/src/main/java/return7/boardbackend/controller/BoardController.java`

#### 변경 사항:

**6.1. 게시글 목록 조회 반환 타입 변경**
- **위치**: `getBoards()` 메서드
- **변경 내용**: 반환 타입을 `List<BoardDto>`에서 `BoardListResponseDto`로 변경
- **주석**: `// FE 작업중 수정 : 반환 타입을 List<BoardDto>에서 BoardListResponseDto로 변경`
- **코드**:
```java
@GetMapping
public BoardListResponseDto getBoards(
        @RequestParam(defaultValue = "0")int page,
        @RequestParam(defaultValue = "10") int size
){
    return boardService.findAll(page,size);
}
```

---

### 7. **BoardListResponseDto.java** (신규 파일)
**경로**: `board-backend/src/main/java/return7/boardbackend/dto/board/BoardListResponseDto.java`

#### 변경 사항:

**7.1. 게시글 목록 응답 DTO 추가**
- **변경 내용**: 게시글 목록과 페이지네이션 정보를 함께 반환하는 DTO 추가
- **코드**:
```java
public record BoardListResponseDto(
    List<BoardDto> items,
    boolean hasNext
) {}
```

---

## 🎯 주요 개선 사항

### 1. 댓글 정렬 기능 개선
- 기본순(오름차순), 최신순(내림차순), 추천순 옵션 제공
- 기본값을 "ascending"으로 변경

### 2. Soft Delete 로직 개선
- 부모 댓글이 soft delete된 상태에서 모든 자식이 삭제되면 부모도 자동 hard delete
- Cleanup API 추가로 데이터 정리 기능 제공

### 3. 페이지네이션 개선
- 게시글 목록 조회 시 `hasNext` 정보 제공
- 정확한 페이지네이션 구현

### 4. 에러 처리 개선
- 채택된 댓글이 없을 때 404 반환
- Optional을 활용한 안전한 null 처리

### 5. JSON 직렬화 일관성
- `isDeleted` 필드에 `@JsonProperty` 추가하여 프론트엔드와의 일관성 보장

---

## 📌 API 변경 사항

### 변경된 엔드포인트

1. **GET /api/boards/{boardId}/replies**
   - `sort` 파라미터 기본값: `"latest"` → `"ascending"`

2. **GET /api/boards/{boardId}/replies/selected**
   - 반환 타입: `ResponseReplyDto` → `Optional<ResponseReplyDto>`
   - 채택된 댓글이 없을 때: 예외 발생 → 404 반환

3. **GET /api/boards**
   - 반환 타입: `List<BoardDto>` → `BoardListResponseDto`
   - 응답 구조: `{ items: [...], hasNext: boolean }`

### 추가된 엔드포인트

1. **POST /api/boards/{boardId}/replies/cleanup**
   - 관리자만 사용 가능
   - Soft delete된 댓글들 중 자식이 모두 삭제된 댓글들을 완전히 삭제
   - 반환: 삭제된 댓글 개수

---

## ⚠️ 주의사항

1. **Cleanup API**는 관리자 권한이 필요합니다.
2. **정렬 기본값 변경**으로 인해 기존 클라이언트와의 호환성에 주의가 필요합니다.
3. **BoardListResponseDto**는 신규 DTO이므로 기존 클라이언트 코드 수정이 필요할 수 있습니다.

---

## 📝 테스트 체크리스트

- [ ] 댓글 정렬 기능 (기본순, 최신순, 추천순)
- [ ] 부모 댓글 자동 삭제 로직
- [ ] Cleanup API 동작 확인
- [ ] 채택된 댓글 조회 404 처리
- [ ] 게시글 페이지네이션 (`hasNext` 확인)
- [ ] `isDeleted` 필드 JSON 직렬화 확인

